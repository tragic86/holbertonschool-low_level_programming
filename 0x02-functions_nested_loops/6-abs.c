#include "holberton.h"
/**
 * _abs - a function that computes the abs value of integer
 *
 * @num: number being used
 *
 * Return: none
 *
 */

int _abs(int num)
{
	if (num < 0)
	{
		num = (-1) * num;
		return (num);
	}
	return (num);

	return (0);


}
